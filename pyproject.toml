[project]
name = "notion-telegram-bot"
version = "0.1.0"
dependencies = [
    "python-telegram-bot==20.3",
    "requests",
    "dateparser",
    "SpeechRecognition",
    "pydub",
    "nest_asyncio"
]

[tool.vercel]
entrypoint = "api.bot:handler"
